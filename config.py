#config settings for psycopg2 access to the database. Assign values to the variables below according to how the db is stored on your setup.

database = 'database_name'
user = 'username'
password = 'password'
